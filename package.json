{
  "name": "pr-review-bot",
  "version": "1.0.0",
  "description": "GitHub webhook server that uses Claude Code to address PR review feedback automatically",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test:webhook": "node scripts/test-webhook.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
